var arr = [1,2,3,4];

// arr.push(5);
// arr.pop();

//arr.length = 0;
//arr.length = 5;
//arr[20] = 20;

//arr.pop();
//arr.shift();
arr.unshift(0);
//arr.splice(arr.length-1, 1);

for(var i=0; i<arr.length; i++) {
    console.log(arr[i]);

    // if(i==2) {
    //     break;
    // }
}

// arr.forEach(function(num) {
//     console.log(num);
//
//     return false;
// });
